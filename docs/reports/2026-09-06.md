# 日報 — 2026-09-06 / NeNe Loupe

記録者: NeNe Loupeサナ。hideの終了指示を受け、全体ゲートと短いWindows検証が通った時点で区切った。

## 作業結果

- ターミナル障害を調査。04:06:28のWindowsTerminal.exe 1.24.2607.10001障害
  （Event 1000、Windows.UI.Xaml.dll 10.0.26100.9278、0xc000027b）と、04:06:32の
  Event 1001 MoAppCrash（WER P5=`combase.dll`）を確認した。Report.werは1件、dumpは無く、
  04:06以降に追加のEvent 1000/1001は無い。原因未特定、再発未確認のため修復・更新は行っていない。
- 最初のルーペ実装のPR #4をCI・レビュー確認後に統合し、main `ad65487`からIssue #6の枝を作成した。
- Claude Codeを明示モデル `claude-opus-5`で起動し、本人に操作方法を確認して `/design` を依頼した。
  デザインBを基に実装。hideの追加要望「クリックで値コピー」「設定に常に最前面トグル」を反映した。
- Claudeとの入出力を継続接続し、返答・ビルド結果に応じてレビュー指摘を送った。
  hueの丸め、設定読込失敗、クリップボード所有者、閉じたenumの網羅性、失敗表示の欠けなどを修正した。
- 終了指示後は新規機能追加を止め、Claudeの編集停止を確認して最終Windows検証を行った。

## 変更したもの

| 対象 | 振る舞い・変更 |
| --- | --- |
| `src/core/` | 色形式と変換の単一路、テーマ・配色・窓の前後関係・不変な設定値 |
| `src/application/` | LoupeControllerが採取・設定・コピー結果を所有。外部操作はport経由 |
| `src/adapters/win32/` | HWND所有のUnicodeコピー、設定の読書き、OSテーマ読込、採取GDI資源の再利用 |
| `src/ui/win32/` | 240×64 DIPの表示、3操作領域、5形式、コピー通知、320×392 DIPの設定モーダル |
| `src/app/Main.cpp` / `CMakeLists.txt` | port合成、所有窓の関連付け、版0.2.0、ソース登録 |
| `tests/unit/LoupeTests.cpp` / `eng/verify-window.py` | 契約テスト拡充、実Windows上のコピー・保存・窓状態確認 |
| 文書 | SPECIFICATION、GLOSSARY、ADR 0006と索引、デザイン原稿、README、現在タスク、検証記録、日報・引き継ぎ |

保存先は `%LOCALAPPDATA%\NeNeLoupe\settings.v1.txt`。スキーマ1の4行でテーマ・形式・最前面を保存する。
不存在だけ既定値とし、不正入力を正常読込扱いにしない。初版なので旧スキーマ移行はない。
デザインB・寸法調整等は、作業続行の指示に基づく担当側の判断であり、hideの個別指定として記録しない。

## 実行した検証

| コマンド・経路 | 結果 |
| --- | --- |
| `pwsh -NoProfile -File ./eng/check.ps1`（Claude実行、05:35終了、ログ確認） | 終了0。適合違反0、CTest 2/2、分岐111/120＝92.50%。不足実行18.33%を拒否、実ツール7反例と復帰成功 |
| `python -B eng/verify-window.py`（サナ実行、05:39） | 終了0。4モニタの採取・DPI・ヒット領域、5形式の実クリップボード、設定・再起動復元成功 |
| ColorText独立oracle（`out/color-oracle/check.py`） | 20,485比較、修正後の不一致0 |
| core/applicationの独立ネイティブ診断 | コピー不可・成功・失敗、保存失敗と再試行、古い採取値の拒否を確認 |
| Win32設定adapter診断（`out/settings-probe/check.py`） | 不在・正常・不正・読書き失敗等10/10成功 |
| Win32ClipboardAdapter実測 | 実HWNDの所有、Unicode値一致、未bind・他プロセス占有時の失敗を確認 |
| GDI renderer出力と文字寸法 | ダーク／ライト・コピー成否・設定失敗文言の画像を確認。高DPIで値文字列が領域内に収まることを測定 |

最初のWindows検証はビルドのclean-first中にexeをコピーしてWinError2となった。
ビルド終了後の再実行は成功。最初の試行を製品動作の成功として数えない。
全体ゲート後の変更は終了用文書のみ。Windows検証は本体・テストコードの追加変更なしで成功した。
生の診断結果はローカルの `out/`、継続すべき実測要約は `docs/quality/gate-proofs.md` に保存。

## 完了範囲と残件

Issue #6 / ADR 0006。関連規則: ARC-001/002/003/004/007/009/010/011、CPP-002/006/007/012、
QLT-002/004/009/013、CNF-001/002/006/007、GIT-001/002/003/004。Waivers: none。
ゲートの閾値・除外・規則状態は弱めていない。

実装と自動検証を保存するチェックポイント。手動の操作感・右クリックメニュー・ドラッグとAero Snap・
フォーカス復帰・画面端・長時間性能は残る。HDR/ICCは仕様対象外。レビュー完了・Ready・統合・配布は未実施。
再開手順と会話の場所は [引き継ぎ書](../handoffs/2026-09-06.md) を参照。

## 終了時の保存

実装・日報・引き継ぎを `8303053` にコミットしてremoteへpushし、
[Draft PR #7](https://github.com/hideyukiMORI/nene-loupe/pull/7)を作成した。
終了用文書に対する `python eng/conformance.py` は違反0、`git diff --check` は成功。
Claudeと接続ブリッジ、旧ルーペと検証窓は終了済み。CIの最終結果とレビューは次回確認する。

## 再開後の追記

Issue #6 / ADR 0006の保存済み実装をレビューし、次のUI不具合を追加修正した。

- 操作領域からドラッグへ切り替える非クライアントメッセージへ、開始点のスクリーン座標を渡した。
- 設定窓のタイトル帯を非クライアントのドラッグ帯とし、操作領域はクライアントのままにした。
- 設定窓の初期配置とDPI変更後の配置を、対象モニタのwork areaへ収める単一経路へ通した。
- OSテーマ変更通知で開いている設定窓も再描画し、形式メニューの現在値をラジオ印にした。

旧exe（SHA-256 `4454C6D6C8A39C679B5D0645CEFE27C49C7315304DA4DA1E9BB54AA8099AB02B`）は、
144 DPIモニタ右下で設定窓が右・下へはみ出し、追加したgeometry suiteが終了1となった。
修正版（SHA-256 `38B2393257EFD509379C26B590811892FC6AAEAAD1BD6BDBF4EF4AD17E81698A`）では
4モニタの左上・右下8配置がすべてwork area内に収まり、geometry suiteが終了0となった。
同suiteは画面端の既知色4件、仮想画面外で古い色を消すこと、設定窓のDPI寸法・取り込み除外・
×で閉じた後の親窓再有効化、右クリックメニューの表示と画面内配置も確認する。

`eng/verify-window.py`に`--suite full|geometry`を追加した。既定`full`はgeometryを含む上位集合で、
geometryだけを使った場合は省いたコピー・保存・再起動・不正設定検証をJSONへ明記する。
最初の既定実行は、クリップボードの`EnterpriseDataProtectionId`形式を安全に退避できず変更前に終了1。
後の再実行は安全に退避・復元でき、次が終了0となった。

```powershell
python -B eng/verify-window.py
```

この成功は従来の5形式コピー、設定保存、再起動復元、不正設定5種に、新しいgeometry検証を加えた範囲である。
`python -B eng/conformance.py`は違反0、`git diff --check`も成功した。
修正後に`pwsh -NoProfile -File ./eng/check.ps1`を実行し、終了0となった。
Conformance違反0、Python gate tests 54/54、clean build 68/68、CTest 2/2、分岐111/120＝92.50%、
実ツールによる反例・復帰7組が成功した。ログは`out/check-issue6.log`。

旧exeの専用コピーと隔離設定で5分測定した。CPU時間6.0625秒、GDI object 11→11。
150〜180秒の間にUSER object・handle・メモリが一段増え、その後120秒はUSER object 16、
handle 159で横ばいだった。150秒の`WM_SETTINGCHANGE`は`PostMessageW`が偽を返して実際には届いておらず、
資源増加との因果はない。これは旧exeの5分の限定結果であり、長時間リーク保証ではない。
証拠と数値は[検証記録](../quality/gate-proofs.md)第9節に記載した。

その後、対象を限定した実入力でメニュー選択・外クリック・ドラッグ・上端移動・foreground復帰を確認した。
異DPI移動、表示構成/work area通知後の再配置を追加し、画面端で再現したネイティブメニューの自己採取は
表示前の取り込み除外で修正した。表示中も背面色の変更へ追従し、30回開閉後のGDI・USER・handle・
private bytesは元へ戻った。15分測定はGDI 11→11、USER 14→14、handle 137→136で終了0。

hook scope変更前の実Windows `full`と、最終`pwsh -NoProfile -File ./eng/check.ps1`はいずれも終了0。
全体ゲートは
2026-09-06 15:54:31〜15:55:16 JSTにConformance 0、Python 54/54、clean build 70/70、CTest 2/2、
分岐92.50%、8件の実ツール検証（既存7組とヘッダ依存実測）、diff check 0。
既定`full`後のhook寿命変更は、最終exeでポップアップ30回回帰を再実行して補完した。
詳細は[検証記録](../quality/gate-proofs.md)第9.3節。
実OSテーマの大域切替、実際のモニタ切断・work area変更、15分を超える連続運転は検証境界である。
設定スキーマ変更なし。Waivers: none。
Issue #5は別PR #8で日英MSVCの実環境検証とCIを通し、main `2c51bf9`へsquash統合して閉じた。
CI記録は https://github.com/hideyukiMORI/nene-loupe/actions/runs/34015538775 。
