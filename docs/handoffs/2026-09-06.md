# 引き継ぎ書 — NeNe Loupe / 2026-09-06終了時点

## 最初に読む情報

- Issue #5、#6、#9、#11、#13、#15はmainへ統合済み。現在はv0.2.0公開準備の
  [Issue #17](https://github.com/hideyukiMORI/nene-loupe/issues/17)、ブランチ`feat/17-release-v0-2-0`を進めている。
- 実装状態: [日報](../reports/2026-09-06.md)、[現在タスク](../todo/current.md)。
- 設計: [ADR 0006](../adr/0006-designed-loupe-controls.md)、[デザイン指示](../design/loupe-look.md)、
  [参考プレビュー](../design/loupe-look-preview.html)、[アプリアイコン](../design/app-icon.md)。
  正本と実装の寸法は設定窓320×392 DIP。

## 再開後レビューの最新状態

保存済み実装のレビューで、ドラッグ開始座標、設定窓のドラッグ帯、OSテーマ通知時の設定窓再描画、
形式メニューのラジオ印、設定窓のDPI/表示構成/work area変更後の画面内配置を修正した。
画面右端で再現したネイティブメニューの自己採取は、表示前に取り込み除外17を設定する単一路で修正した。
`eng/verify-window.py`は`--suite full|geometry`を受け、既定`full`はgeometryを含む上位集合である。
実入力でメニュー選択・外クリック・ドラッグ・上端移動・foreground復帰を確認し、異DPI移動、
同期通知後の再配置、メニュー表示中の背面色追従、30回開閉、15分の資源測定も終了した。
既定`full`はhook寿命を狭める直前の同機能実装で終了0、scope変更後は最終exeのメニュー30回回帰で
補完した。最終全体ゲートも終了0。設定スキーマ変更なし。Waivers: none。
数値、SHA-256、負例と成功例は[検証記録](../quality/gate-proofs.md)第9.3節を参照する。

## 次回の作業順

1. [Issue #17](https://github.com/hideyukiMORI/nene-loupe/issues/17)と対応PRを正本として、未完了の工程だけ進める。
2. 最終HEADの全体ゲートとCI成功後にsquash統合し、cleanなmainからRelease成果物を生成する。
3. ZIP、checksum、埋込版、x64、静的runtime、短い起動を確認してから`v0.2.0`を公開する。

```powershell
git status --short --branch
pwsh -NoProfile -File ./eng/check.ps1
python -B eng/verify-window.py
```

変更が生じた場合だけ最後の2コマンドを直列に実行する。`verify-window.py`の既定は全検証の`full`。
クリップボードを変更できない状況で画面配置だけを診断するときは`--suite geometry`を明示し、
省略項目を成功扱いしない。全体ゲートはclean-firstでexeを削除して作り直すため、実機検証のexeコピーと
競合させない。実機検証はロックされていない対話デスクトップが必要。

## 確認済みと未確認の境界

最終全体ゲートは2026-09-06 15:54:31〜15:55:16 JSTに終了0。Conformance 0、Python 54/54、
clean build 70/70、CTest 2/2、分岐92.50%、8件の実ツール検証（既存7組とヘッダ依存実測）、diff check 0。
実Windows `full`はhook scope変更前の同機能実装で4モニタ、画面端、設定、popup、クリップボードを
含めて終了0。scope変更後は最終exeのpopup30回回帰で補完した。
実OSテーマの大域切替と、実際のモニタ切断・work area変更は行っていない。system appearanceの実読込、
単体テストのdark/light切替、実メッセージによる再描画、synthetic通知後の再配置までを確認した。
資源測定の観測上限は15分。それを超える連続運転の保証ではない。詳細は
[検証記録](../quality/gate-proofs.md)第9.3節。

## 実装の要点

- 色変換の唯一の実装は `core/ColorText`、状態所有者は `application/LoupeController`。
- Clipboard/SettingsStore/SystemAppearance portをappでWin32 adapterに結線する。
- ルーペ240×64 DIP、レンズ7×7画素を8倍。中心は(32,32) DIP。
- チップ(86,3)-(150,27)、ギア(211,3)-(238,27)、値(86,29)-(238,61)が操作領域。
  採取失敗中はチップ・値がドラッグ領域に戻り、ギアは有効。
- 形式はHEX → CMYK → HSL → HSV → RGB → HEX。保存の初期値はsystem/hex/topmost。
- 設定モーダルは失敗文言の行を加えて高さ392 DIP。図形アイコンはGDIで描く。
- コピーは所有HWNDを関連付け、メモリ確保後にクリップボードを空にする。失敗を成功表示しない。
- スキーマ1は `%LOCALAPPDATA%\NeNeLoupe\settings.v1.txt` の4行。
  `schema=1`、`theme=dark|light|system`、`format=rgb|hex|cmyk|hsl|hsv`、`layer=topmost|normal`。
  保存は同じディレクトリの一時ファイルから置換。旧版移行は未定義。

## Claude Codeと証拠の場所

使用モデルは明示ID `claude-opus-5`。デザイン生成後、同じ会話を継続して実装を依頼した。
セッション: `79a6b671-0cc8-4b1e-b98e-5f77f1f82e48`。
必要なときだけリポジトリ内で次を実行して再開する。

```powershell
claude --resume 79a6b671-0cc8-4b1e-b98e-5f77f1f82e48 --model claude-opus-5
```

`/design`はネイティブ対話セッションで利用した。print呼出しだけでスキルが利用できると仮定しない。
今回のリアルタイム接続は以下のローカルディレクトリに保存した。

`C:\Users\info\.claude\jobs\neneloupe-implementation-20260906\`

- `messages.jsonl`: テキスト・ツール呼出し・完了の要約。
- `events.jsonl`: ツール結果を含む生ログ。05:35の最終ゲートで `gate exit=0`。
- `inbox/*.sent`: サナから送ったレビュー・指示。`021-checkpoint.sent`が終了時の指示。
- `bridge.py` / `process.json`: 継続接続の実装と最後のプロセス情報。PIDは過去の記録であり再利用しない。

Claudeは05:37に編集・ビルド停止を報告済み。サナが完了結果を受領してClaudeプロセスを終了した。
旧ルーペと検証窓も終了した。再開には新たな起動が必要で、バックグラウンド継続を前提にしない。

再開後はNeNe LoupeリナをClaude Opus 5で新規起動し、ネイティブ`/design`で今回の3つのUI判断を相談した。
標準ラジオ印とOS標準メニュー寸法、設定窓の上端ドラッグ帯、最終DPIでのwork areaクランプはいずれも
既存デザインBに適合するとの回答だった。コード編集・ビルド・外部送信は行わせていない。

元デザイン: `C:\Users\info\.claude\jobs\4e1da9a1\tmp\design\`。
[Claudeデザイン成果物](https://claude.ai/code/artifact/c87169a5-06d1-4fe5-b554-729e3f1e1a7a)。
長期保存する判断と指示はリポジトリの `docs/design/` とADRへ取り込み済み。
追加診断・画像・生のWindows結果は `out/` 以下に残るがGit対象外。そこだけを正本にしない。

## 規則・保留

Issue #6、ADR 0006。関連規則とファイル一覧は日報に記載。Waivers: none。
ゲートの緩和・新しい除外なし。設定スキーマ変更なし。Issue #5はPR #8でmainへ統合済み。
ターミナル障害は04:06のEvent 1000/1001とReport.wer 1件を確認したがdumpは無く、原因・再発とも未確認。
根拠なく修復・更新せず、再発時にイベント対とdump有無を採取する。全体ゲートは終了0。
当時のReady・統合判断はPR #7のGitHub上の状態に沿って行った。

## Issue #11 再開点

- 正本は[Issue #11](https://github.com/hideyukiMORI/nene-loupe/issues/11)と対応PR。ブランチは
  `feat/11-flat-checker-icon`。
- [アプリアイコン設計](../design/app-icon.md)とSVG、派生ICO、通常build外のgeneratorを更新した。
  Win32のresource ID、HICON所有、失敗処理は変更していない。
- ICOは8サイズ。2回再生成のSHA-256が一致し、隔離build 71/71、conformance 0、PE/Shell/HICONと
  起動smokeが成功した。数値は[検証記録](../quality/gate-proofs.md)第11節。
- 最終HEADで`pwsh -NoProfile -File ./eng/check.ps1`を1回実行し、同じHEADをReady/CIへ進める。
- 統合後は検証済みexeのSHA-256を保存する。起動中の利用者exeを正規終了し、単一Python threadの
  PMv2 contextを確認して新exeを起動する。実矩形が240×64 DIP相当かつwork area内、設定ファイルの
  前後SHA-256不変を実再読値で確認する。
- スタートアップ登録、ショートカット生成、通知領域常駐、AppUserModelID追加は範囲外。
  設定スキーマ変更なし。Waivers: none。

## Issue #13 再開点

- 正本は[Issue #13](https://github.com/hideyukiMORI/nene-loupe/issues/13)と対応PR。ブランチは
  `feat/13-bold-loupe-icon`。
- 背景4マスを維持し、正本SVGと派生ICOの前景ルーペだけを大きく太くした。generatorとWin32経路は不変。
- 8サイズの再生成一致、隔離build 71/71、conformance 0、PE/Shell/HICONと起動smokeが成功した。
  数値は[検証記録](../quality/gate-proofs.md)第12節。
- 最終HEADで正規全体ゲートを1回実行し、同じHEADをReady/CIへ進める。統合後はPMv2を確認した
  単一Python threadで、利用者の現在位置と設定SHAを保って検証済みexeへ入れ替える。
- 設定スキーマ変更なし。Waivers: none。

## Issue #15 再開点

- 正本は[Issue #15](https://github.com/hideyukiMORI/nene-loupe/issues/15)と対応PR。ブランチは
  `feat/15-unified-icon-silhouette`。
- 背景4色を維持し、前景をring/stem一体形、外枠を見える幅1.5へ洗練した。generatorとWin32経路は不変。
- 8サイズの再生成一致、隔離build 71/71、conformance 0、PE/Shell/HICONと起動smokeが成功した。
  数値は[検証記録](../quality/gate-proofs.md)第13節。
- 最終HEADで正規全体ゲートを1回実行し、同じHEADをReady/CIへ進める。統合後は利用者の直前位置と
  設定SHAを保って検証済みexeへ入れ替える。
- 設定スキーマ変更なし。Waivers: none。

## Issue #17 再開点

- 正本は[Issue #17](https://github.com/hideyukiMORI/nene-loupe/issues/17)と対応PR。版入力は
  `CMakeLists.txt`のPROJECT_VERSIONだけとし、manifestとVERSIONINFOをbuild下へ生成する。
- `eng/package-release.ps1`が固定toolchainと既存CMake targetをReleaseでclean buildし、
  `NeNeLoupe.exe`、`README.txt`、`LICENSE`のportable ZIPと`SHA256SUMS`を作る。
- 実装中のRelease narrow buildは71/71、build graph conformance 0、CTest 2/2。manifest、
  VERSIONINFO、x64、静的runtime、ZIP内容を確認した。ZIP展開exeのPMv2起動smokeも終了0。
- 最終全体ゲートとCIはIssue #17のPRを正本として確認する。公開成果物のSHA-256はmerged mainから
  作り直すGitHub Releaseの`SHA256SUMS`を正本とする。設定スキーマ変更なし。Waivers: none。
