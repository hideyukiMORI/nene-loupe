# 仕様 — NeNe Loupe

> Status: normative（規範）/ 2026-09-06 初版（施主の口述を FR に起こしたもの）
> 「決め打ちしない」項目は明示する。実装して窮屈なら変えてよい。変えたら本書と ADR を更新する。

## 0. 一言

枠の無い小さな窓。左がルーペ、右がその中心の色番号。クリックでコピー。

## 1. 機能要件

| ID | 要件 | 備考 |
| --- | --- | --- |
| FR-001 | 窓は枠・タイトルバーを持たない | `WS_POPUP` |
| FR-002 | 窓は本体のどこを掴んでもドラッグで移動できる | 操作部品の上は除く |
| FR-003 | 左側にルーペを表示する。ルーペ領域を透かした位置の背面を拡大して描く。採取中心はルーペの中央に固定し、マウス位置に追従しない | 自分の窓を取り込みから除外し、背面を `BitBlt` で取得する。拡大は最近傍（ぼかさない） |
| FR-004 | ルーペの中心ピクセルを示す印を描く | 十字か枠 |
| FR-005 | 右側にルーペ中心の色を表示形式に従って表示する | 例: `255, 128, 0` / `#FF8000` |
| FR-006 | 色番号の表示をクリックすると、**表示しているその文字列**をクリップボードへコピーする | 成功も失敗も短く示す。表示とコピーは同じ文字列 |
| FR-007 | 表示形式を切り替えられる。閉じた集合: RGB 10 進・RGB 16 進・CMYK（素朴換算）・HSL・HSV | 集合は `enum class`。増やすときは仕様と `switch` の両方が変わる |
| FR-008 | 切替の操作は「クリックで巡回するトグル」＋「右クリックのポップアップメニュー」 | 🔶 **決め打ちしない**。ドロップダウンにするかは実装後に判断 |
| FR-009 | ギアアイコンで設定モーダルを開く | 2 枚目のポップアップ窓 |
| FR-010 | 設定モーダルで、アプリのカラーリング（テーマ）を選べる | 閉じた集合 |
| FR-011 | 設定モーダルにコピーライトと版を表示する | 版はビルド時に 1 か所から埋め込む |
| FR-012 | 設定は再起動をまたいで保存される | 保存先は adapters/win32。形式は版を持つ（ARC-009）。版 1 は `%LOCALAPPDATA%\NeNeLoupe\settings.v1.txt` |
| FR-015 | 窓を常に最前面にするかどうかを設定で切り替えられる | 閉じた集合。既定は最前面。その場で反映し、再起動をまたいで保存する |
| FR-016 | 設定の読み込み・保存の失敗と、コピーの失敗を利用者に見える形で示す | 既定値へ黙って落とさない（ARC-009） |
| FR-013 | 表示は Per-Monitor v2 の DPI で崩れない | マニフェスト |
| FR-014 | 複数モニタで、どのモニタの上でもルーペが正しい画素を拾う | 仮想スクリーン座標 |

## 2. 決め打ちしない項目

| 項目 | 初期値 | 変えてよい条件 |
| --- | --- | --- |
| 窓のサイズ | **240×64**（ADR 0006 で確定）。160×64 では FR-006〜009 の置き場が無かった | 窮屈なら大きくしてよい（施主 2026-09-06） |
| ルーペの倍率 | 8 倍 | 実機で見て決める |
| 切替 UI（FR-008） | トグル＋右クリック（ADR 0006 で採用） | 実装後の使用感 |
| 更新の刻み | 30 ms 前後 | CPU 使用率を測ってから |

## 3. 非要件（やらないこと）

- ICC プロファイルを使った正確な CMYK。**素朴換算**であることを UI か README に書き、盛らない（設定モーダルに明記することを要求する）
- HDR / 広色域の値。`BitBlt` が返す sRGB 8 bit を扱う
- 画面の録画・保存。取り込んだ画素は表示にだけ使い、ディスクに書かない
- 常駐・ホットキー・トレイ常駐（要るなら ADR）
- UI ライブラリ（WinUI 3 / WPF / Qt 等）。ADR 0002

## 4. 設計上の固定点（憲章との対応）

- ルーペの採取座標と画面の取り込みは、画素を得る**唯一の入力経路**であるポート（`ScreenSamplerPort`）から入る。利用者の操作や保存済み設定も外部入力であり、それぞれの境界で受ける（ARC-007）
- 表示更新のタイマーは**再描画のきっかけ**であって、値の正本ではない。毎回サンプリングし直す（NeNeClock SWG-005 と同型）
- 色の値型と変換・整形は `core` に置き、`windows.h` を含まない。ここだけ厚くテストする（ARC-003）
- 現在のサンプル・表示形式・テーマの所有者は `application` に 1 つ（ARC-004）

## 5. 対応環境

Windows 10 version 2004以降（x64）、DWM有効。ルーペ自身の取り込みを防ぐため
`WDA_EXCLUDEFROMCAPTURE`を使う（ADR 0005）。このため、一般の画面キャプチャにも本窓は映らない。
2026-09-06、hideの実機確認に基づきFR-003の採取中心をルーペの中央へ修正した。
同日、ADR 0006で窓を240×64 DIPに広げると決め、FR-006〜012とFR-015/016を規範に加えた。
設定モーダルもルーペ窓と同じく取り込みから除外することを要求する。実装の完了は実測で別に記録する。
